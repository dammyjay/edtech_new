<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= (info?.company_name || 'JKT Hub').trim() %></title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/styles2.css" />
    <link rel="stylesheet" href="/css/styles3.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="icon" type="image/x-icon" href="/images/JKT logo bg.png" />
    <script src="https://cdn.ckeditor.com/4.21.0/standard/ckeditor.js"></script>
    <script src="/js/uiAlerts.js"></script>
</head>
<body>
        <!-- Header -->
    <header class="header">
      <div class="logo-title">
        <% info = info || {}; %> <% if (info.logo_url) { %>
        <img src="<%= info.logo_url %>" alt="Logo" />
        <% } %>
        <p><%= (info?.company_name || 'JKT Hub').trim() %></p>
      </div>
       <button id="nav-toggle-btn" class="nav-toggle" onclick="toggleNav()" aria-expanded="false" aria-controls="main-nav">☰</button>
      <nav id="main-nav">
        <a href="/">Home</a>
        <% if (role === 'admin') { %>
          <div class="dropdown">
          <button class="dropbtn" onclick="toggleDropdown('dashboardMenu')">
              Dashboard ▾
            </button>
            <div class="dropdown-content" id="dashboardMenu">
              <a href="<%= role === 'admin' ? '/admin/dashboard' : '/instructor/dashboard' %>">
                Dashboard
              </a>
              <a href="/admin/db">Database</a>
              <a href="/admin/analytics">Analytics</a>
              <a href="/admin/feedback">Feedbacks</a>
            </div>
        </div>

          <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('studentsMenu')">
              Students ▾
            </button>
            <div class="dropdown-content" id="studentsMenu">
              <a href="/admin/students">All Students</a>
              <a href="/admin/students/progress">Progress & Performance</a>
              <a href="/admin/students/enrollments">Enrollments</a>
              <a href="/admin/parent-invoices">Parent Invoice</a>
            </div>
          </div>

          <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('schoolsMenu')">
              Schools ▾
            </button>
            <div class="dropdown-content" id="schoolsMenu">
              <a href="/admin/schools">All Schools</a>
              <a href="/admin/quotes">School Quotes</a>
              <a href="/admin/school-courses">Assign Courses</a>
            </div>
          </div>
        <% } %>

        <% if (role === 'instructor') { %>
          
          <a href="/instructor/dashboard">Dashboard</a>
          <a href="/instructor/chats">Chats</a>
        <% } %>
        <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('resourceMenu')">
              Resource ▾
            </button>
            <div class="dropdown-content" id="resourceMenu">
              <a href="/admin/pathways">Pathways</a>
              <a href="/admin/courses">Courses</a>
              <a href="/admin/guides">Guides</a>
            </div>
          </div>
        


        <% if (role === 'admin') { %>
          <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('galleryMenu')">
              Gallery ▾
            </button>
            <div class="dropdown-content" id="galleryMenu">
              <a href="/admin/gallery">Gallery</a>
            </div>
          </div>

          <div class="dropdown">
            <button
              class="dropbtn"
              onclick="toggleDropdown('ministryMenu')"
            >
              company ▾
            </button>
            <div class="dropdown-content" id="ministryMenu">
              <a href="/admin/company">Company Details</a>
              <a href="/admin/about">About Us</a>
              <a href="/admin/benefits">Benefits</a>
              
            </div>
          </div>

          <div class="dropdown">
            <button class="dropbtn" onclick="toggleDropdown('communicationMenu')">
              Communication ▾
            </button>
            <div class="dropdown-content" id="communicationMenu">
              <a href="/admin/faqs">FAQ</a>
              <a href="/admin/testimonies">Testimonies</a>
              <a href="/admin/events">Events</a>
              <a href="/admin/newsletters">Newsletters</a>
              <a href="/admin/announcements">Announcements</a>
            </div>
          </div>
        <% } %>

        <%- include('./notificationBell') %>
        <a href="/profile">Profile</a>
        <a href="/admin/logout" class="logout-button">Logout</a>
      </nav>
    </header>

    <script>
      function toggleNav() {
        var nav = document.getElementById("main-nav");
        nav.classList.toggle("open");
      }

    </script>

<script>
  // Keep toggleNav globally so existing inline onclicks still work
  window.toggleNav = function toggleNav() {
    const nav = document.getElementById('main-nav');
    const btn = document.getElementById('nav-toggle-btn');
    if (!nav || !btn) return;
    nav.classList.toggle('open');
    btn.setAttribute('aria-expanded', nav.classList.contains('open'));
  };

  // Close nav when clicking outside (robust)
  document.addEventListener('click', function (event) {
    const nav = document.getElementById('main-nav');
    const btn = document.getElementById('nav-toggle-btn');
    if (!nav || !btn) return;

    // if nav is closed, nothing to do
    if (!nav.classList.contains('open')) return;

    // if click occurred inside nav or on the toggle button — ignore
    if (nav.contains(event.target) || btn.contains(event.target)) return;

    // otherwise close nav
    nav.classList.remove('open');
    btn.setAttribute('aria-expanded', 'false');
  }, true); // use capture to make this more robust

  // Optional: close nav with Escape key
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') {
      const nav = document.getElementById('main-nav');
      const btn = document.getElementById('nav-toggle-btn');
      if (nav && nav.classList.contains('open')) {
        nav.classList.remove('open');
        if (btn) btn.setAttribute('aria-expanded', 'false');
      }
    }
  });
</script>

<script>
  // Dropdown menus (Dashboard/Students/Schools/etc.) previously only opened
  // via CSS :hover, so the onclick="toggleDropdown(...)" on every .dropbtn
  // called a function that was never defined — touch devices had no way to
  // open them at all. This makes tap/click actually work everywhere.
  window.toggleDropdown = function toggleDropdown(id) {
    const menu = document.getElementById(id);
    if (!menu) return;
    const isOpen = menu.classList.contains('show');
    document.querySelectorAll('.dropdown-content.show').forEach(function (el) {
      el.classList.remove('show');
    });
    if (!isOpen) menu.classList.add('show');
  };

  document.addEventListener('click', function (event) {
    if (event.target.closest('.dropdown')) return;
    document.querySelectorAll('.dropdown-content.show').forEach(function (el) {
      el.classList.remove('show');
    });
  });

  // Highlight whichever nav/dropdown link matches the current page, and
  // light up the parent dropdown button so e.g. "Students ▾" shows as
  // active while browsing any page under Students.
  (function highlightActiveNav() {
    var path = window.location.pathname.replace(/\/+$/, '') || '/';
    var best = null, bestLen = -1;
    document.querySelectorAll('#main-nav a[href]').forEach(function (link) {
      var href = link.getAttribute('href');
      if (!href || href.charAt(0) !== '/') return;
      var normalized = href.replace(/\/+$/, '') || '/';
      var matches = normalized === '/'
        ? path === '/'
        : (path === normalized || path.indexOf(normalized + '/') === 0);
      if (matches && normalized.length > bestLen) {
        best = link;
        bestLen = normalized.length;
      }
    });
    if (best) {
      best.classList.add('active');
      var content = best.closest('.dropdown-content');
      var dropdown = content && content.closest('.dropdown');
      var btn = dropdown && dropdown.querySelector('.dropbtn');
      if (btn) btn.classList.add('active');
    }
  })();
</script>

<script>
document.addEventListener("DOMContentLoaded", () => {

  document.querySelectorAll(".sortable-table").forEach(table => {

    const headers = table.querySelectorAll("thead th");

    headers.forEach((header, columnIndex) => {

      header.classList.add("sortable");

      let ascending = true;

      header.addEventListener("click", () => {

        const tbody = table.querySelector("tbody");
        const rows = Array.from(tbody.querySelectorAll("tr"));

        // Remove previous sort indicators
        headers.forEach(h => {
          h.classList.remove("sort-asc", "sort-desc");
        });

        rows.sort((a, b) => {

          let aText =
            a.children[columnIndex]?.innerText.trim() || "";

          let bText =
            b.children[columnIndex]?.innerText.trim() || "";

          // Numeric sorting
          const aNum =
            parseFloat(aText.replace(/[^\d.-]/g, ""));

          const bNum =
            parseFloat(bText.replace(/[^\d.-]/g, ""));

          if (!isNaN(aNum) && !isNaN(bNum)) {
            return ascending
              ? aNum - bNum
              : bNum - aNum;
          }

          // Date sorting
          const aDate = Date.parse(aText);
          const bDate = Date.parse(bText);

          if (!isNaN(aDate) && !isNaN(bDate)) {
            return ascending
              ? aDate - bDate
              : bDate - aDate;
          }

          // Text sorting
          return ascending
            ? aText.localeCompare(bText)
            : bText.localeCompare(aText);

        });

        tbody.innerHTML = "";
        rows.forEach(row => tbody.appendChild(row));

        header.classList.add(
          ascending ? "sort-asc" : "sort-desc"
        );

        ascending = !ascending;
      });

    });

  });

});
</script>

<!-- CRUD feedback toast: any redirect can append ?toast=...&toastType=success|error
     and this shows it once, then strips it from the URL so a refresh doesn't
     re-show it. -->
<style>
  #admin-toast {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 99999;
    max-width: 360px;
    padding: 14px 18px;
    border-radius: 10px;
    font-size: 14px;
    font-weight: 600;
    color: #fff;
    box-shadow: 0 6px 20px rgba(0,0,0,0.25);
    display: flex;
    align-items: center;
    gap: 10px;
    opacity: 0;
    transform: translateY(-12px);
    transition: opacity 0.25s ease, transform 0.25s ease;
    pointer-events: none;
  }
  #admin-toast.show {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }
  #admin-toast.success { background: #1f9d55; }
  #admin-toast.error { background: #d64545; }
  #admin-toast button {
    background: none;
    border: none;
    color: #fff;
    font-size: 16px;
    line-height: 1;
    cursor: pointer;
    margin-left: auto;
    opacity: 0.85;
  }
  #admin-toast button:hover { opacity: 1; }
</style>
<div id="admin-toast" role="status" aria-live="polite">
  <span id="admin-toast-message"></span>
  <button type="button" onclick="document.getElementById('admin-toast').classList.remove('show')">&times;</button>
</div>
<script>
  (function () {
    var params = new URLSearchParams(window.location.search);
    var message = params.get('toast');
    if (!message) return;

    var type = params.get('toastType') === 'error' ? 'error' : 'success';
    var toast = document.getElementById('admin-toast');
    document.getElementById('admin-toast-message').textContent = message;
    toast.classList.add(type, 'show');

    setTimeout(function () {
      toast.classList.remove('show');
    }, 4500);

    // Strip toast params from the URL so a page refresh doesn't re-show it.
    params.delete('toast');
    params.delete('toastType');
    var query = params.toString();
    var cleanUrl = window.location.pathname + (query ? '?' + query : '') + window.location.hash;
    window.history.replaceState({}, '', cleanUrl);
  })();
</script>
</body>
</html>